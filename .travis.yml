language: node_js
node_js:
  - "8"

install: npm install

script:
  - npm run test

cache:
  directories:
  - node_modules

jobs:
  include:
    - stage acs
      script cd app/templates/adf-cli-acs-template && npm install && npm test
